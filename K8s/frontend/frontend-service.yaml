apiVersion: v1
kind: Service
metadata:
  name: zb-frontend-service
spec:
  selector:
    app.kubernetes.io/name: zero-broker
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4000
