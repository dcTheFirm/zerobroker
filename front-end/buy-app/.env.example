# Example .env for buy micro-frontend (copy to .env and edit)
# The buy app uses the Buy backend for buy-listings features.
VITE_BUY_BASE_URL=http://localhost:4003

# Optional dev variables:
# VITE_AUTH_BASE_URL=http://localhost:5000
# VITE_SEARCH_BASE_URL=http://localhost:4004

# Notes:
# - Replace with your deployed endpoints when building for prod.
# - Restart Vite/dev server after editing .env.
