# Example .env for search micro-frontend (copy to .env and edit)
# The search app uses the Search backend to run property searches.
VITE_SEARCH_BASE_URL=http://localhost:4004

# Optional dev variables you may also set:
# VITE_AUTH_BASE_URL=http://localhost:5000
# VITE_HOME_BASE_URL=http://localhost:4001

# Notes:
# - Set to your deployed search backend URL for production builds.
# - Restart the dev server after updating .env.
