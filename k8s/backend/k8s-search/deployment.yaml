apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-search      
  namespace: zero-broker
  labels:
    app: backend-search

spec:
  replicas: 2

  selector:
    matchLabels:
      app: backend-search

  template:
    metadata:
      labels:
        app: backend-search

    spec:
      containers:
        - name: backend-search
          image: dcthefirm/backend-search:f6e2c5e5f8d6115115698e22d85f1514fcdbf7ba

          ports:
            - containerPort: 4004

          envFrom:
            # - configMapRef:
            #     name: auth-configmap
            - secretRef:
                name: mongo-secret

          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi

          readinessProbe:
            httpGet:
              path: /health
              port: 4004
            initialDelaySeconds: 5
            periodSeconds: 10

          livenessProbe:
            httpGet:
              path: /health
              port: 4004
            initialDelaySeconds: 15
            periodSeconds: 20