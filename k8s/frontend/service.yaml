apiVersion: v1
kind: Service
metadata:
  name: zerobroker-frontend-service
  namespace: zero-broker
spec:
  selector:
    app: zerobroker-frontend
  ports:
    - protocol: TCP
      port: 5173
      targetPort: 80
